{
  "name": "train-detection-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "deploy": "npm install --omit=dev && ZIPFILE=\"deployment_$(date +%m_%d_%Y_%H_%M_%S).zip\" && zip -r \"$ZIPFILE\" . --exclude '*.git*' --exclude '.env' --exclude '*.zip' --exclude '*.sql' --exclude 'local_dump*' --exclude 'remote_dump*' && aws lambda update-function-code --function-name train-detection-express --region us-west-2 --profile train-detection-deploy --zip-file \"fileb://$ZIPFILE\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.787.0",
    "@aws-sdk/s3-request-presigner": "^3.787.0",
    "cors": "^2.8.6",
    "dotenv": "^16.5.0",
    "express": "^5.2.1",
    "multer": "^2.0.0",
    "pg": "^8.13.3",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
